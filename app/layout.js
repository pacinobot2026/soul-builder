export const metadata = {
  title: 'SOUL.md Builder',
  description: 'Build your AI agent personality - OpenClaw Shadow Workshop',
}

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  )
}
